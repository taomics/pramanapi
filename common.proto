syntax = "proto3";

package taomics.praman.common;

option go_package = "github.com/taomics/pramanapi;common";

enum Sex {
  UNKNOWN = 0;
  MALE = 1;
  FEMALE = 2;
}

message Date {
  uint32 year = 1;
  uint32 month = 2;
  uint32 day = 3;
}
